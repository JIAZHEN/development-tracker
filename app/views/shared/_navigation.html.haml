%nav.navbar.navbar-default.navbar-static-top{:role => "navigation", :style => "margin-bottom: 0"}
  .navbar-header
    %button.navbar-toggle{"data-target" => ".navbar-collapse", "data-toggle" => "collapse", :type => "button"}
      %span.sr-only Toggle navigation
      %span.icon-bar
      %span.icon-bar
      %span.icon-bar
    %a.navbar-brand{:href => root_path} Development Tracker
  / /.navbar-header
  %ul.nav.navbar-top-links.navbar-right
    %li.dropdown
      - if authenticated?
        %a.dropdown-toggle{"data-toggle" => "dropdown", :href => "#"}
          = image_tag(client.user.avatar_url, :size => "20x20")
          = client.user.name
          %i.fa.fa-caret-down
        %ul.dropdown-menu.dropdown-user
          %li
            %a{:href => logout_path, "data-method" => "delete"}
              %i.fa.fa-sign-out.fa-fw
              Logout
      - else
        %a{:href => login_path} Login with Github

  - if authenticated?
    .navbar-default.sidebar{:role => "navigation"}
      .sidebar-nav.navbar-collapse
        %ul#side-menu.nav
          %li
            %a{:href => new_release_path}
              %i.fa.fa-dashboard.fa-fw
              Make a Release Request
          %li
            %a{:href => releases_path}
              %i.fa.fa-cog.fa-fw
              Releases
